/*
 * Code generated by Speakeasy (https://speakeasyapi.dev). DO NOT EDIT.
 */

import * as components from "../components";
import * as z from "zod";

/**
 * An unexpected error response.
 */
export type PaymentErrorData = {
    errorCode: components.PaymentErrorCodes;
    errorMessage: string;
};

/**
 * An unexpected error response.
 */
export class PaymentError extends Error {
    errorCode: components.PaymentErrorCodes;
    errorMessage: string;

    /** The original data that was passed to this error instance. */
    data$: PaymentErrorData;

    constructor(err: PaymentErrorData) {
        super("");
        this.data$ = err;

        this.errorCode = err.errorCode;
        this.errorMessage = err.errorMessage;

        this.message =
            "message" in err && typeof err.message === "string"
                ? err.message
                : "API error occurred";

        this.name = "PaymentError";
    }
}

/** @internal */
export namespace PaymentError$ {
    export const inboundSchema: z.ZodType<PaymentError, z.ZodTypeDef, unknown> = z
        .object({
            errorCode: components.PaymentErrorCodes$.inboundSchema,
            errorMessage: z.string(),
        })
        .transform((v) => {
            return new PaymentError(v);
        });

    export type Outbound = {
        errorCode: string;
        errorMessage: string;
    };

    export const outboundSchema: z.ZodType<Outbound, z.ZodTypeDef, PaymentError> = z
        .instanceof(PaymentError)
        .transform((v) => v.data$)
        .pipe(
            z.object({
                errorCode: components.PaymentErrorCodes$.outboundSchema,
                errorMessage: z.string(),
            })
        );
}
